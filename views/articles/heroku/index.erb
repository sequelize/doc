<%
  sections = [{
    :title => 'Getting started with Heroku',
    :short => 'Getting started'
  }, {
    :title => 'A minimal express application',
    :short => 'Minimal express app'
  }, {
    :title => 'Deployment to Heroku',
    :short => 'Deployment'
  }, {
    :title => 'Spawning a database on Heroku',
    :short => 'Spawn the database'
  }, {
    :title => 'Adding Sequelize to the application',
    :short => 'Adding Sequelize'
  }, {
    :title => 'tl;dr',
    :short => 'tl;dr'
  }]
%>

<header class="jumbotron subhead" id="overview">
  <div class="row">
    <div class="span12">
      <h2>
        Sequelize on Heroku
        <small>using PostgreSQL</small>

        <a href="http://flattr.com/thing/1259407/Sequelize" target="_blank" style="float: right; position: relative; top: 10px;">
          <img src="http://api.flattr.com/button/flattr-badge-large.png" alt="Flattr this" title="Flattr this" border="0" />
        </a>
      </h2>
      <p class="lead">
        This section covers the use of Sequelize on Heroku. It will explain how to get started with Heroku and what is necessary
        to setup a proper environment. We will use MySQL on the development machine and PostgreSQL on the remote servers.
      </p>
    </div>
  </div>
</header>

<% sections.each do |section| %>
  <% partial_name = section[:short].downcase.gsub(/\W/, '_') %>

  <section>
    <div class="page-header">
      <h3 data-nav-value="<%= section[:short] %>"><%= section[:title] %></h3>
    </div>
    <%= markdown("articles/heroku/_#{partial_name}".to_sym) %>
  </section>
<% end %>
