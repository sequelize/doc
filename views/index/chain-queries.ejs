Because you will want to save several items at once and just go on after all of them are saved, Sequelize provides a handy helper for that:</p>
<pre><%- koala(".js", partial("code/chain-queries/chain-queries-1.ejs")) %></pre>

<a name="parallel"></a>
<p>And a real example:</p>
<pre><%- koala(".js", partial("code/chain-queries/chain-queries-2.ejs")) %></pre>

<a name="serially"></a>

<p>It is also possible to force a serial run of the query chainer by using the following syntax:</p>
<pre><%- koala(".js", partial("code/chain-queries/serial.ejs")) %></pre>
